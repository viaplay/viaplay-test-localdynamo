module.exports = function() {
	return function( callback, successFn ) {
		return function( err ) {
			if( err )
				if( err.rerouted && err.reroutes ) {
					err.reroutes.push( module.parent.filename );
					callback( err );
				}
				else
					callback( { rerouted: true, reroutes: [ module.parent.filename ], err: err } );
			else
				successFn.apply( undefined, Array.prototype.slice.call( arguments ).slice( 1 ) );
		};
	};
};
